import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wireloopescape.app',
  appName: 'wire-loop-escape',
  webDir: 'dist'
};

export default config;
